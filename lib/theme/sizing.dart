import 'package:flutter/foundation.dart' show kIsWeb;

class CrossPlataformFontSize {
  static const double bodyText = kIsWeb ? 45 : 25;
  static const double introPadding = kIsWeb ? 100 : 50;
  static const double sizedBoxHeight = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
  // static const double bodyText = kIsWeb ? 45 : 25;
}
